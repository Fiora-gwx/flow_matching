# Stage 2 follow-up template:
# 1. Read experiments/results/ft_clock_variance_reduction_ablation/plots/ablation_best_strategy_by_beta_nfe.csv
# 2. Pick the winning strategy fields for each beta you want to promote.
# 3. Replace the example strategy block below, then run this config.
experiment_name: ft_clock_variance_reduction_ablation_followup
base_config:
  dataset: cifar10
  data_path: ./data/cifar10
  epochs: 500
  batch_size: 128
  num_gpus: 2
  accum_iter: 2
  path_family: linear
  cfg_scale: 0.2
  class_drop_prob: 0.2
  metrics: [fid]
  eval_epochs: [499]
  eval_nfes: [4, 8, 16, 32, 100]

experiments: []
